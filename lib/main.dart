import 'package:flutter/material.dart';
import 'package:order_master/screen/login_screen.dart'; // Importe a tela de login.
void main() {
  runApp(MaterialApp(
    home: LoginScreen(), // Use LoginScreen como a tela inicial do aplicativo.
  ));
}
